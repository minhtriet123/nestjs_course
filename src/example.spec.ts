function addNums(n1, n2) {
  return n1 + n2;
}
describe('addNums', () => {
  it('add two numbers', () => {
    expect(addNums(2, 3)).toEqual(5);
  });
});
describe('addNums', () => {
  it('add two numbers', () => {
    expect(addNums(2, 10)).toEqual(12);
  });
});
describe('addNums', () => {
  it('add two numbers', () => {
    expect(addNums(2, 10)).toEqual(12);
  });
});
describe('addNums', () => {
  it('add two numbers', () => {
    expect(addNums(5, 5)).toEqual(10);
  });
});
describe('addNums', () => {
  it('add two numbers', () => {
    expect(addNums(9, 10)).toEqual(19);
  });
});
